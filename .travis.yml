sudo: required
language:
  - cpp
os:
  - windows

script:
  - choco install -y msys2
  - pacman -V
  - cmake --version
  - mkdir build
  - cd build
  - cmake -G 'MinGW Makefiles' -DCMAKE_SH="CMAKE_SH-NOTFOUND" ..
  - cmake --build .
